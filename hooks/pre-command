#!/bin/bash
set -euov pipefail

echo "--- :julia: Instantiating project"

julia --project -e 'using Pkg;
                    Pkg.instantiate();
                    Pkg.precompile();
                    Pkg.status()'
